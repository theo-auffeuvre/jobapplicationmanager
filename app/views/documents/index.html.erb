<div class="grid grid-cols-4 gap-5 mx-40  my-8" id="documents">
  <%= link_to new_document_path, data: { turbo_frame: "modal" }, id:"add_doc" do %>
    <%= tag.li class: "flex flex-col justify-center border-dashed my-2 rounded shadow-md border px-3 py-2 text-gray-400 bg-gray-100 h-full w-fit min-w-full min-h-56" do %>
      <div class="flex flex-col justify-center items-center h-full">
        <i class="fas fa-plus text-3xl"></i>
        <p>New Document</p>
      </div>
    <% end %>
  <% end %>
  <% @documents.each do |document| %>
      <%= render partial: "document", locals: { document: document } %>
  <% end %>
</div>
