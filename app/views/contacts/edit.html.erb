<%= turbo_frame_tag "modal" do %>
  <div class="fixed top-0 left-0 w-screen h-screen bg-sky-600 bg-opacity-80 flex" data-controller="turbo-modal" data-action="turbo:submit-end->turbo-modal#submitEnd">
    <div class="m-auto  align-middle w-3/5 h-fit bg-white opacity-100 rounded-lg z-50">
      <div class="flex">
        <div class="flex flex-col w-full">
          <%= render "contacts/edit_form" %>
        </div>
        <%= render "contacts/add_job_to_contact" %>
      </div>
    </div>
  <div>
<% end %>
